# AGENTS.md · Agent 工作指导

本文件面向在本仓库及两个赛题工作区（`dpa4c-nano-lammps-md/`、`llm-challenge-arena-v1/`）中工作的 AI Agent。仓库层面的协作约定见 `README.md`，比赛规则见 `docs/比赛规则说明.md`；本文件只讲**怎么干活**。

## 卡住时：跳出局部，不要重复尝试

这是最重要的一条。解题是长链路任务，最大的浪费不是某一步失败，而是**在同一个地方反复失败**。

**触发信号**：同一个报错、同一个失败测试、同一段代码，已经连续尝试了 **3 次**仍无实质进展（错误信息没有变化，或只是表面在变、根因没动）。

**触发后必须停下重试，依次做这几件事**：

1. **陈述根因假设**：用一两句话写下「我认为真正的问题是 X，证据是 Y」。写不出来，说明信息不够，先补信息（加日志、缩小复现、读报错全文），而不是再改一版碰运气。
2. **跳出局部搜索资料**：读官方文档、源码、issue、论文，而不是继续凭直觉改。PPU SDK、vLLM/SGLang、LAMMPS/DeePMD 的行为差异多半有人踩过坑；本仓库 `docs/` 和各赛题工作区的 OpenSpec 变更里也可能已有记录，先查再试。
3. **从全局任务角度重新审视**：
   - 这个目标本身对吗？（例如：在死磕一个对综合加速比贡献很小的算子，而别处有更大的热点）
   - 有没有绕过它的路？（换方案、换工具、换粒度，而不是在同一方案里打转）
   - 当前做法是否违反了题面红线和评分口径？（回看 `task.md` / 题面，确认没有在优化一个不算分的指标）
4. **换一条明显不同的路再试**：新一轮尝试必须基于新的假设或新的信息，与失败的前几轮有可说的区别。如果只能说「再试一次」，那就还没准备好再试。

**禁止的行为**：

- 同一条命令/同一处修改**不改任何变量地重跑**，指望结果不同
- 报错只看最后一行就动手改，不读完整堆栈和日志
- 为了「先让这一步过」而绕过校验、注释掉测试、放宽精度门槛——这在比赛里是红线行为

## 过程记录

- 尝试过的方案和结论（含失败结论）随手记入各赛题工作区的 `optimization.md` / OpenSpec 变更，**避免几小时后重复踩同一个坑**。
- 每完成一个阶段按题面要求向选手同步进展；长时间静默摸索不符合参赛要求。

## 资源意识

- PPU 算力稀缺（每人不超过 1 卡）、每天提交次数有限（DPA 赛每天最多 5 次）。规划实验时按稀缺资源对待：能本机/CPU 验证的不上卡，能小规模验证的不直接跑全套。
- 卡或额度被一次卡住的实验长时间占用时，主动暂停、释放资源、想清楚再上。
